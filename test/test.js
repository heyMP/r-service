var assert = require('assert');
describe('baseline', function() {
  describe('example1', function() {
    it('it should do simple equations', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});